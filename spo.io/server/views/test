<h2>Top Artistas:</h2>
    <% topArtists.forEach(artist => { %>
        <div>
            <img src="<%= artist.imageUrl %>" alt="Imagem do Artista" width="50" height="50">
            <p><%= artist.name %></p>
        </div>
    <% }) %>

    <h2>Top Músicas:</h2>
    <% topTracks.forEach(track => { %>
        <div>
            <img src="<%= track.imageUrl %>" alt="Capa do Álbum" width="50" height="50">
            <p><%= track.name %> - <%= track.artistName %></p>
        </div>
    <% }) %>